/**
 * The template's heart field, read from the web client rather than copied, so
 * both apps draw the same pattern. See frontend/src/lib/heart-field.ts.
 */
export * from '../../../frontend/src/lib/heart-field';
